/*
Package config contains Synse Plugin SDK configuration definitions. It defines
versioned configuration schemes, configuration parsing and building. Both plugin
configuration and device configuration are handled here.
*/
package config
