version: 3
debug: true
network:
  type: unix
  address: dynamic-registration-plugin.sock
settings:
  read:
    interval: 5s
  write:
    interval: 5s
  transaction:
    ttl: 8m
dynamicRegistration:
  config:
    - base: 10
    - base: 13
health:
  # disable health file for examples
  healthFile: ""